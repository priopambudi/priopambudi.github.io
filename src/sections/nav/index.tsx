import './navbar.css';

const Navbar = () => {
  return (
    <nav className="container mx-auto">
      <a href="/" className="available-work">
        <span></span>Available for work
      </a>
    </nav>
  );
};

export default Navbar;
